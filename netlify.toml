[build]
  publish = "."

# Service Worker nie aggressiv cachen (sonst kommen Updates nicht an)
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json"
